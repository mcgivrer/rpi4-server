# README Docker Gitlab Portainer Traefik

To up this docker compose recipe, define previously the `SITE_DOMAIN` variable 
to point to the root host domain, in the file `.env`.



then

```bash
$> docker-compose up -d
```


McG.

